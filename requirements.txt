# Core
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
PyYAML>=6.0

# HTTP client for model providers (Ollama, OpenAI-compatible APIs)
httpx>=0.27

# Vision (CV stack)
opencv-python>=4.9
numpy>=1.26
Pillow>=10.0
mss>=9.0

# Input (Windows DirectX-friendly)
pydirectinput>=1.0.4

# OCR — выбор после первой пробы (RapidOCR / PaddleOCR), добавим в следующей итерации
