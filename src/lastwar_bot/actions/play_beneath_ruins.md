# Play the seasonal descent mini-game «Beneath the Ruins» on its own, N rounds deep.
# ru: Сыграть в сезонную мини-игру «Под руинами» самостоятельно, N раундов.

ARGS rounds = 5

# The match IS this recipe's window, and it goes on playing after the recipe has
# armed it. So the reward-popup ear is told to close nothing while it runs (#2027):
# the screens here are not on its whitelist either, and this is the lock that does
# not depend on that list staying right. The hold carries its own deadline — half an
# hour, longer than any match — because nobody is left here to lift it.
TAP hold_reward_popups
READ_LUA (function() local ok, res = pcall(function() for _, nd in ipairs((__lw_gg or {}).nodes or {}) do pcall(function() UpdateBeat:RemoveListener(nd) end) end __lw_gg = nil local r = CS.UnityEngine.Object.FindObjectOfType(typeof(CS.MiniGame.GGGo.Client.GGGoRuntime)) if r ~= nil then return 'open' end local M = package.loaded['DataCenter.SmallGameManager.SmallGameManager'] local g = M:GetGameByType(4) local d = DataCenter.LWGGGoDataManager if d == nil or d.data == nil or d.data.bid == nil then return 'no round data' end g:ChangeSource(2) g:UpdateGGGoInfo(d.data) g:Enter() return 'opening' end) return tostring(ok) .. ' :: ' .. tostring(res) end)() INTO door
LOG "beneath-ruins: {door}"
WAIT 12
READ_LUA (function() local ok, res = pcall(function() local FO = CS.UnityEngine.Object.FindObjectOfType local T = typeof local V2 = CS.UnityEngine.Vector2 local Time = CS.UnityEngine.Time for _, nd in ipairs((__lw_gg or {}).nodes or {}) do pcall(function() UpdateBeat:RemoveListener(nd) end) end __lw_gg = nil local r = FO(T(CS.MiniGame.GGGo.Client.GGGoRuntime)) local m = FO(T(CS.MiniGame.GGGo.Client.UIGGGoMain)) if r == nil or m == nil then return 'no window' end local g = package.loaded['DataCenter.SmallGameManager.SmallGameManager']:GetGameByType(4) local function num(v) local t = tostring(v) local x = t:match('^%-?%d+%.?%d*') return x and (x + 0) or 0 end local S = {n = 0, nodes = {}, off = false, err = '', rounds = 0, want = {rounds}, best = 0, last = 0, r = r, m = m, g = g, p = nil, root = nil, dir = 0, live = false, log = {}} __lw_gg = S local function dynroot() local root = S.r.LevelRootGO if root == nil then return nil end local t = root.transform for i = 0, t.childCount - 1 do local c = t:GetChild(i) for j = 0, c.childCount - 1 do local d = c:GetChild(j) if d.name:find('Dynamic') then return d end end if c.name:find('Dynamic') then return c end end return nil end local function push(sx) if sx == S.dir and (S.age or 0) - (S.said or -99) < 15 then return end S.dir = sx S.said = S.age or 0 if sx == 0 then S.m:OnJoystickEnd(V2(0, 0)) else S.m:OnJoystickMove(V2(sx, 0)) end end local function step(now) local st = tostring(S.r.Game.State) if st:find('Running') then S.live = true end if S.live and (st:find('Settlement') or st:find('Preparing')) then S.live = false local dv = num(S.r.Game.Env.Distance) S.last = dv if dv < S.best then S.best = dv end S.rounds = S.rounds + 1 S.log[#S.log+1] = 'round ' .. tostring(S.rounds) .. '=' .. string.format('%.2f', dv) S.p = nil S.root = nil S.dir = 0 if S.rounds >= S.want then S.off = true pcall(function() S.g:Exit() end) return end pcall(function() S.g:Again() end) return end if not st:find('Running') then return end S.age = (S.age or 0) + 1 local ok_p = false if S.p ~= nil then local a, v = pcall(function() return S.p.gameObject.activeInHierarchy end) ok_p = a and v == true if ok_p and (S.still or 0) > 20 then ok_p = false S.p = nil S.still = 0 end end if not ok_p and S.age - (S.hunt or -99) > 20 then S.hunt = S.age local ps = CS.UnityEngine.Object.FindObjectsOfType(T(CS.MiniGame.GGGo.Client.UIGGGoPlayerController)) S.p = nil for i = 0, ps.Length - 1 do if ps[i].gameObject.activeInHierarchy then S.p = ps[i] end end ok_p = S.p ~= nil end if not ok_p then return end if S.root == nil or S.root.childCount == 0 then S.root = dynroot() end if S.root == nil then return end local v = S.p.transform.position if S.lx ~= nil and v.x == S.lx and v.y == S.ly then S.still = (S.still or 0) + 1 else S.still = 0 end S.lx = v.x S.ly = v.y local best, bestcost = nil, 999 for i = 0, S.root.childCount - 1 do local c = S.root:GetChild(i) local n = c.name if not n:find('Spike') then local q = c.position local dy = v.y - q.y if dy > 0.35 and dy < 3.2 then local dx = q.x - v.x if dx < 0 then dx = -dx end local cost = dx + dy * 0.6 if n:find('Disappear') then cost = cost + 0.4 end if cost < bestcost then bestcost = cost best = q end end end end if best == nil then push(0) return end local dx = best.x - v.x if dx > 0.12 then push(-1) elseif dx < -0.12 then push(1) else push(0) end end local deadline = Time.realtimeSinceStartup + 90 + 120 * S.want local f = function() local t = __lw_gg if t == nil or t.off then return end t.n = t.n + 1 if t.n % 2 ~= 0 then return end if Time.realtimeSinceStartup > deadline then t.off = true t.err = 'deadline' return end local a, e = pcall(step, Time.realtimeSinceStartup) if not a then t.err = tostring(e) t.off = true end end local node = UpdateBeat:CreateListener(f, S) S.nodes = {node} UpdateBeat:AddListener(node) if tostring(r.Game.State):find('Settlement') then pcall(function() S.g:Again() end) end return 'armed' end) return tostring(ok) .. ' :: ' .. tostring(res) end)() INTO armed
LOG "beneath-ruins: {armed}"
